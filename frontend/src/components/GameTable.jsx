import React from 'react';
import { Box } from '@mui/material';

/**
 * Main poker table layout component.
 * Arranges the game elements: opponent panel (top), community cards (center),
 * pot display, player panel (bottom), and action controls.
 */
function GameTable({ game, onAction }) {
  // TODO: Render poker table layout with:
  //   - Opponent PlayerPanel at top (cards face-down unless showdown)
  //   - Community cards (CardDisplay) in center row
  //   - Pot size display in center
  //   - Player PlayerPanel at bottom (cards face-up)
  //   - ActionControls below player panel
  //   - Current phase indicator
  //   - Dealer button indicator

  return (
    <Box>
      {/* TODO: Implement poker table layout */}
      <p>GameTable - TODO</p>
    </Box>
  );
}

export default GameTable;
