spring:
  application:
    name: poker-tutor
  ai:
    vertex:
      ai:
        gemini:
          project-id: ${GCP_PROJECT_ID:jking-ai-labs}
          location: ${GCP_REGION:us-central1}
    model:
      chat: vertexai

server:
  port: 8080

# TODO: Configure Firebase settings
# firebase:
#   project-id: ${FIREBASE_PROJECT_ID}
#   database-url: https://${FIREBASE_PROJECT_ID}-default-rtdb.firebaseio.com

app:
  cors:
    allowed-origins:
      - http://localhost:5173
